# ============================================================
# Live Multimodal Monitoring System — Backend Dependencies
# Phase 1: Core backend + database + API
# Phase 2: Video pipeline (OpenCV + image processing)
# ============================================================

# --- Web framework ---
fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9      # file uploads

# --- Pydantic ---
pydantic==2.7.4
pydantic-settings==2.3.4

# --- Database (async SQLAlchemy + SQLite for Phase 1) ---
sqlalchemy==2.0.31
aiosqlite==0.20.0            # async SQLite driver
alembic==1.13.2

# --- Config ---
python-dotenv==1.0.1
pyyaml==6.0.2

# --- Phase 2: Image / video processing (lightweight) ---
opencv-python==4.10.0.84
Pillow==10.4.0
numpy==1.26.4

# --- Testing ---
pytest==8.3.2
pytest-asyncio==0.23.8
httpx==0.27.0

# ============================================================
# Phase 3–5 ML dependencies → install from requirements-ml.txt
# pip install -r requirements-ml.txt
# ============================================================
