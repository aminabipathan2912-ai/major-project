# ============================================================
# ML-specific dependencies for phases 3–5
# Install with: pip install -r requirements-ml.txt
# WARNING: torch alone is ~2 GB. Only install when running
# the actual ML pipelines (audio, NLP, trainable fusion).
# ============================================================

# Deep learning framework
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# Hugging Face ecosystem
transformers==4.41.2
tokenizers==0.19.1
huggingface-hub==0.23.4
datasets==2.20.0
accelerate==0.31.0

# Audio processing (Phase 3)
librosa==0.10.2
soundfile==0.12.1

# Classical ML (Phases 5–6 fusion)
scikit-learn==1.5.0
xgboost==2.0.3

# Additional utilities
einops==0.8.0          # tensor ops for transformers
timm==1.0.7            # pretrained vision models
